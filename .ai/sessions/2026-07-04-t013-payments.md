# Session 2026-07-04 — T-013 Payments

## What was done
- Models: Payment (application PROTECT, server-side amount ETB, method
  telebirr/cbe_birr/chapa/cash, status pending/paid/failed/cancelled,
  gateway_ref indexed, unique receipt_no, paid_at, raw_webhook snapshot,
  initiated_by) + ReconciliationBatch (date range, totals JSON, run_by).
  Both audited. Receipt seq abis_receipt_no_seq → RCP-YYYY-NNNNNN.
- Providers: PaymentProvider ABC (create_checkout/parse_webhook/
  verify_signature = HMAC-SHA256 raw body, constant-time compare) +
  lru-cached get_provider from ABIS_PAYMENT_PROVIDERS; SandboxProvider
  (deterministic SBX-XXX refs + fake checkout urls) for all three gateways
  (ADR-022). Per-provider secrets ABIS_PAYMENT_WEBHOOK_SECRETS (env).
- services: initiate_payment (submitted-only, already-paid 400, idempotent
  pending reuse, cash settles immediately), process_webhook (provider 404 →
  signature 403 → JSON 400 → unknown ref 404 → paid replay idempotent →
  amount mismatch 400 → paid/_settle | failed), _settle (receipt + paid_at +
  registration.services.mark_paid when application is submitted; warning
  log on odd states), run_reconciliation (by_method/by_status/paid_total/
  paid-but-app-not-paid mismatches → persisted batch).
- Endpoints: initiate (201/200), webhook (AllowAny + HMAC + `webhook`
  throttle scope 120/min), payments read-only list w/ filters+search,
  reconcile POST → 201 batch, reconciliations list. PaymentPermission
  op/sup/admin.
- Settings: fee/providers/secrets + webhook throttle (base, test, .env.example).

## Verification (all green)
- pytest **303/303** (21 new), coverage 98%, spectacular exit 0, style clean.
- **Webhook flips application to paid** (receipt RCP-, paid_at, app status).
- **Bad signature 403**; missing signature 403; cross-provider secret 403
  (telebirr webhook signed with chapa secret rejected).
- Unknown ref/provider 404; amount mismatch 400 (payment stays pending);
  failed status → payment failed, app stays submitted; replay idempotent
  (receipt unchanged). Initiate: draft 400, repeat returns same pending
  (200), post-paid 400, cash immediate settle + app paid. Reconciliation
  totals + persistence + RBAC.
- Note: raw-Decimal in a plain dict response renders as JSON float —
  test compares via Decimal; PaymentSerializer (list/detail) renders
  amounts as strings normally.

## Next
- T-014 clearance + verification: decision endpoint (in_review→approved|
  rejected — but flow needs paid→biometrics_captured→in_review transitions;
  add operator/system endpoints or wire enrollment completion),
  reportlab PDF certificate + qrcode + verification_no, public verify
  (masked) + API-key verify + VerificationEvent log.
