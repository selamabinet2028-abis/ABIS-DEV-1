# Session 2026-07-04 — T-003 Dev Scripts + docker-compose

## What was done
- `docker-compose.yml` (root): `db` postgres:16 → host **5433** (ADR-011),
  `redis` redis:7 → 6379, named volumes, healthchecks (pg_isready / redis-cli
  ping). Container names abis-db / abis-redis.
- `scripts/common.ps1` — dot-sourced helpers: repo paths, Write-Step/Ok/Warning2,
  `Resolve-Python312` (py -3.12 → uv Astral 3.12 → PATH fallback),
  `Start-DevServices` (compose up + block until healthy, 90 s deadline),
  `Resolve-Shell` (pwsh → powershell).
- `scripts/setup.ps1` — idempotent: prereq check, venv create (3.12), pip
  install, npm install, .env from example, services up, migrate, seed_demo
  guarded via `manage.py help --commands` (skips until T-019).
- `scripts/dev.ps1` — services up, then 3 titled windows: runserver :8000,
  celery `--pool=solo`, vite :5173.
- `scripts/test.ps1` — 6 gates with summary + exit code: pytest, vitest,
  typecheck, lint, pip-audit, `npm audit --audit-level=critical`.
- Port 5433 propagated to `backend/.env.example` and `base.py` default.
- README: fresh-clone quickstart (prereqs, what setup does, endpoints table).

## Verification (all green)
- `setup.ps1` end-to-end twice (fresh services + idempotent re-run, exit 0);
  all migrations applied to dockerized Postgres (accounts, auth,
  token_blacklist, admin, sessions).
- Live `runserver` boot against docker db: health `ok v1.0.0`, Swagger 200
  — closes the T-001 deferred live-boot check.
- Celery worker boots to `ready.` on redis://localhost:6379/0 (pool=solo).
- `test.ps1`: **all 6 gates PASS**, exit 0.

## Issues hit & fixed
1. **PS 5.1 encoding:** BOM-less UTF-8 .ps1 with em-dashes → parser errors.
   Fixed: ASCII-only + UTF-8 BOM (ADR-012).
2. **Docker Desktop auto-updated mid-pull** (29.5.3 → 29.6.1): engine pipe
   vanished, "unexpected EOF" on postgres:16 pull. Restarted Docker Desktop,
   re-pulled, fine. If pulls die suddenly, check whether Docker just updated.
3. **`*> $null` + $ErrorActionPreference=Stop** turns native stderr into a
   terminating error in PS 5.1 → seed_demo guard now uses `help --commands`.
4. **pip-audit caught Pillow 11.3.0** (7 CVEs, incl. CVE-2026-25990 etc.) —
   bumped to `Pillow>=12.2,<13.0` (12.3.0 installed), gate now green.

## Next
- T-004 accounts app (custom User fields, RBAC classes, auth endpoints with
  **httpOnly refresh cookie** per ADR-006 — frontend T-002 already expects it;
  after T-004, re-verify the live login round-trip and update T-002 note).
