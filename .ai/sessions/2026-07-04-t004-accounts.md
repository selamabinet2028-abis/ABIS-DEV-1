# Session 2026-07-04 — T-004 Accounts (RBAC + Auth)

## What was done
- `common/models.py` (plain package): UUIDModel/TimeStampedModel/BaseModel
  abstract bases per DATABASE_DESIGN (UUID pks + timestamps).
- `basedata.OrgUnit` minimal (name, parent self-FK) — User.org_unit needs it;
  T-006 extends. Migration basedata.0001.
- accounts models: `Permission` (codename/name/module catalog), `Role`
  (5 fixed names, M2M permissions), `User` extended (role PROTECT FK,
  org_unit SET_NULL FK, badge_number unique, phone, must_change_password,
  password_changed_at, failed_login_attempts, locked_until, `is_locked`
  property), `UserActivityLog` (auth events, SET_NULL user + username string).
  Migrations 0002 + 0003 (data seed: 15 permissions, 5 roles with sensible
  permission sets — admin gets all).
- `services.py`: log_activity, register_failed/successful_login (lockout:
  threshold 5 → lock 15 min, counter resets on lock and on success),
  blacklist_user_tokens, refresh-cookie set/clear/read helpers.
- `permissions.py`: RolePermission base (deny-by-default, superuser bypass) +
  IsAdmin/IsOperator/IsInvestigator/IsSupervisor (admin included in each) +
  IsAuditorReadOnly (SAFE_METHODS only).
- `views.py`: LoginView (lockout check → authenticate → activity log → access
  + user + httpOnly `abis_refresh` cookie, scoped throttle `auth`),
  RefreshView (cookie or body fallback; rotation sets new cookie; explicit
  401 on TokenError), LogoutView (blacklist + clear cookie, 205),
  PasswordChangeView (validators, blacklists all outstanding tokens),
  UserViewSet (admin CRUD; DELETE=deactivate+token blacklist; /me/;
  /activity/ admin|auditor-ro; search username/email/name/badge),
  RoleViewSet (409 on delete-in-use), PermissionViewSet (read-only).
- Settings: `ABIS_AUTH` config, `auth: 10/min` throttle rate,
  CORS_ALLOW_CREDENTIALS; test settings: high throttle rates (view-level
  ScopedRateThrottle ignores empty DEFAULT_THROTTLE_CLASSES), longer test
  SECRET_KEY (PyJWT HMAC length warning).
- API_DOCUMENTATION auth + accounts sections rewritten as-built (ADR-013).

## Verification (all green)
- pytest **63/63**; `--cov=apps` **98%**.
  - test_auth: login ok/wrong/unknown/inactive; lockout at threshold + blocked
    with correct password + expiry unlock; refresh rotation + old-token 401 +
    no-cookie 401 + body fallback; logout blacklist + cookie clear + 205;
    password change (auth required, wrong current, weak policy, full rotation).
  - test_rbac: deny-by-default matrix (users/roles/permissions endpoints ×
    5 roles + anon + role-less user + superuser bypass), activity endpoint
    admin+auditor only, /me/ for all authenticated.
  - test_users_api: create w/ role + password policy, search by badge, PATCH
    role, PATCH cannot set password, DELETE deactivates + login blocked,
    activity rows, seeded roles content, role delete 409 in use.
- `spectacular --validate --fail-on-warn` exit 0. black/isort/ruff clean.
- **Live HTTP round-trip** (docker db, real runserver): login → access+user,
  no refresh in body, abis_refresh httpOnly path=/api/v1/auth/; /users/me/
  with Bearer OK; refresh rotates; logout 205; refresh-after-logout 401.
  → closes the T-002 deferred item.

## Gotchas recorded
- DRF maps AuthenticationFailed→403 on views with `authentication_classes=[]`
  (no WWW-Authenticate); RefreshView returns explicit 401 (ADR-013).
- View-level ScopedRateThrottle still applies when DEFAULT_THROTTLE_CLASSES
  is emptied in tests — must also override DEFAULT_THROTTLE_RATES.
- Dev admin user created locally: admin / Adm1n!Passw0rd42 (dev db only;
  seed_demo T-019 will own demo credentials).

## Next
- T-005 audit app (insert-only AuditLog + middleware/signals + read endpoint).
