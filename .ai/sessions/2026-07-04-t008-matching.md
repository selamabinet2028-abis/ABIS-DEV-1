# Session 2026-07-04 — T-008 Matching Engine + Jobs

## What was done
- `matching/engines/base.py`: MatchingEngine ABC (extract, similarity,
  verify, identify, dedup — identify/dedup generic in base, overridable),
  `get_engine()` lru-cached import from `MATCHING_ENGINE` setting (ADR-004).
- `matching/engines/mock.py`: MockEngine over GRID16 templates —
  similarity = 100·(1 − meanAbsDiff/255); identical images → 100.0,
  distinct noise ≈ 66 → clean separation at default threshold 80.
- Models: MatchJob (job_type 7 choices, probe_record / **probe_enrollment**
  (DEDUP, ADR-017; probe_latent lands T-009), status w/ (status, created_at)
  index, threshold, requested_by, timings, error), MatchCandidate (job,
  person, record, score, rank unique-per-job, decision, verified_by,
  decided_at). Both audited (ABIS_AUDITED_MODELS).
- `services.py` (documented cross-app API): start_identify_job,
  start_dedup_job, verify_person_record (sync, still writes done VERIFY job),
  execute_job (Celery entry), decide_candidate; galleries decrypt templates
  on the fly, exclude probe record / probe person (dedup), skip deleted
  persons; candidates created row-by-row so audit signals fire.
- `tasks.py` run_match_job; dispatch = eager-aware (`on_commit` in real
  deployments — pytest eager mode calls delay directly since on_commit
  callbacks never run inside wrapped test transactions).
- Endpoints: POST /match/identify/ (202 {job_id}; validates modality vs
  job_type, template presence; LT-*/VERIFY/DEDUP rejected w/ explanations),
  POST /match/verify/ (sync {match, score, job_id}), GET /match/jobs[/{id}]
  (nested ranked candidates), POST /match/candidates/{id}/decision/.
  RBAC: MatchingRunPermission (inv/sup/admin), CandidateDecisionPermission
  (inv/admin — supervisor 403 verified).
- Enrollment `trigger_dedup` stub replaced → start_dedup_job; T-007 ten-print
  test updated (dedup_job_id now real, done, 0 candidates for fresh person).
- Settings: MATCHING_ENGINE, ABIS_MATCH_THRESHOLD=80, ABIS_MATCH_TOP_K=20.

## Verification (all green)
- pytest **174/174** (31 new), coverage **98%**, spectacular clean, style clean.
- **Dedup flags duplicate person**: same image enrolled under person B →
  DONE job, exactly 1 candidate = person A, score 100, rank 1, best record;
  unique person → 0 candidates; person enrolled twice aggregates to ONE
  candidate row; requested_by defaults to enrollment operator.
- **Identify returns ranked candidates**: 202 → poll done; rank 1 = duplicate
  record @100; scores descending; threshold 99 filters all; FACE-1N matches
  faces and rejects finger probes; unknown record 404.
- **Engine contract (9 tests)**: deterministic extract, identical=100,
  symmetry, verify accept/reject, ranking + threshold + top_k, dedup
  aggregation, garbage template → 0.0. These gate future SDK engines.
- Decisions: hit recorded w/ verified_by + decided_at; 'undecided' rejected;
  RBAC matrix on jobs list; operator identify 403; anon 401.

## Deferred (tracked)
- LT-TP/LT-LT execution + TP-LT latent gallery → T-009 (probe_latent FK).
- WebSocket ws/jobs/{id}/ push → T-011/T-018 (polling contract works now).

## Next
- T-009 investigation app (Case, LatentPrint w/ minutiae + enhance ops,
  EvidenceDocument, latent searches). Add probe_latent FK to MatchJob,
  extend execute_job for LT-*; add models to ABIS_AUDITED_MODELS.
