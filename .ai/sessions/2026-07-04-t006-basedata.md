# Session 2026-07-04 — T-006 Basedata (Person cards + base data)

## What was done
- `Person`: person_no `P-YYYY-NNNNNN` via PG sequence `abis_person_no_seq`
  (migration 0003 RunSQL; services.generate_person_no), Ethiopian name
  convention (first/middle=father/last=grandfather), gender choices,
  nationality, unique-nullable `national_id_no` (Fayda FIN; '' coerced to
  NULL in serializer), `addresses` JSONField (list-of-objects validated) with
  **GIN index**, photo ImageField (per-person folder, uuid filename),
  remarks, `is_deleted` soft delete (destroy sets flag; queryset excludes).
- `OrgUnit` CRUD (self-parent rejected), `LookupValue` (category+code unique,
  ?category= filter), `InvestigationCategory` (unique code, is_active).
- Photo upload `POST /persons/{id}/photo/`: multipart, extension whitelist
  jpg/jpeg/png, `ABIS_MAX_UPLOAD_MB` cap (default 5), Pillow verification via
  ImageField (binary masquerading as .jpg → 400).
- Search auditing: PersonViewSet.list logs `AuditLog SEARCH` (query + actor)
  when `?search=` present; plain lists not logged.
- All four models appended to `ABIS_AUDITED_MODELS`.
- `RoleMatrixPermission` (accounts.permissions): read_roles vs write_roles
  matrix base. PersonPermission (read: admin/operator/investigator/supervisor;
  write: admin/operator — auditor has NO person access per seed permissions),
  BaseDataAdminWrite (read: all staff roles; write: admin).
- `django.contrib.postgres` added to INSTALLED_APPS (GinIndex).

## Verification (all green)
- pytest **124/124** (34 new), `--cov=apps` **99%**, spectacular
  --fail-on-warn exit 0, black/isort/ruff clean.
- Persons: create w/ generated number + audit row; unique sequential numbers;
  update audit diff; soft delete keeps row + hides from list; NULL national-id
  coexistence + duplicate 400; addresses shape validation; RBAC matrix
  (auditor read 403!); anon 401; search by name/person_no/national_id;
  search audit row w/ actor; photo happy path + fake-image 400 + bad
  extension 400 + oversize 400 + investigator 403.
- Base data: org hierarchy + parent filter + self-parent 400 + non-admin
  write 403 + audited create; lookups category filter + duplicate 400 +
  operator write 403; categories active filter + duplicate 400.

## Notes
- Photo serving: dev uses MEDIA_URL static serving; prod (T-021) must proxy
  /media/ via nginx with auth considerations (photo URLs are not public).
- `GET /biometric-records/{id}/image/` style protected file serving pattern
  will be needed in T-007 — consider X-Accel-Redirect in prod.

## Next
- T-007 enrollment + preprocessing (Enrollment, BiometricRecord, encrypted
  BiometricTemplate w/ Fernet key env `ABIS_FIELD_KEY`, quality scoring).
  Remember: add models to ABIS_AUDITED_MODELS; multipart capture endpoint
  per modality/position; reject below quality threshold.
